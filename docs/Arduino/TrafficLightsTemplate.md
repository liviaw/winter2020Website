```java
// template for traffic lights activity

int red = 7;
int yellow = 6;
int green = 5;

void setup() {
  // initialize the pins as outputs
  pinMode(red, OUTPUT);
  pinMode(yellow, OUTPUT);  
  pinMode(green, OUTPUT);
}

void loop () {
  // TODO: make the leds turn on and off
  
}

```
